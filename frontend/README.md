# Frontend

Static assets only (served by the Flask app from `backend/server.py`).

| File        | Purpose                          |
|------------|-----------------------------------|
| `index.html` | Pipeline UI (date range, run)   |

No Python here — all API and data logic lives under **`../backend/`**.
